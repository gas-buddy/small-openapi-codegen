{{#if allOf}}
export type {{@key}} =
{{#forEach allOf}}
{{>type}}{{#unless isLast}} & {{/unless}}
{{/forEach}}
{{else}}
export interface {{@key}} {
{{#each (properties .)}}
  {{name}}{{#unless isRequired}}?{{/unless}}: {{>type .}};
{{/each}}
}
{{/if}}